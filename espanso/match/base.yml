# Automatically generated by espanso migration tool
# Original file: default.yml

matches:
  - trigger: ":espanso"
    replace: Hi there!
  - trigger: ":date"
    replace: "{{mydate}}"
    vars:
      - name: mydate
        type: date
        params:
          format: "%m/%d/%Y"
  - trigger: ":bc"
    word: true
    replace: "{{output}}"
    vars:
      - name: output
        type: shell
        params:
          cmd: "echo `date -v -1d \"+[[%Y-%m-%d|<< Yesterday]]\"`' | '`date -v +1d \"+[[%Y-%m-%d|Tommorrow >>]]\"`"
  - triggers:
      - ":<w"
      - ":<fri"
    word: true
    replace: "{{output}}"
    vars:
      - name: output
        type: shell
        params:
          cmd: "echo `date -v -Fri \"+[[%Y-%m-%d|<< Last Week]]\"`' | '`date -v +1d \"+[[%Y-%m-%d|Tommorrow >>]]\"`"
  - trigger: ":meet"
    word: true
    replace: |
      ## 0000 Meeting
      - Tags:
      Time: 
      Meeting Link:
      Attendees:
      Tasks:
      Notes:
  - triggers:
      - ":>w"
      - ":>mon"
    word: true
    replace: "{{output}}"
    vars:
      - name: output
        type: shell
        params:
          cmd: "echo `date -v -1d \"+[[%Y-%m-%d|<< Yesterday]]\"`' | '`date -v +Mon \"+[[%Y-%m-%d|Next Week >>]]\"`"
  - triggers:
      - ":>2w"
    word: true
    replace: "{{output}}"
    vars:
      - name: output
        type: shell
        params:
          cmd: "echo `date -v +2w \"+[[%Y-%m-%d|Two Weeks >>]]\"`"
  - trigger: ":>fri"
    word: true
    replace: "{{output}}"
    vars:
      - name: output
        type: shell
        params:
          cmd: "echo `date -v +Fri \"+[[%Y-%m-%d]]\"`"
  - trigger: ":>standup"
    word: true
    replace: |
      *What did you do since your last post?*
      *What do you plan on doing next?*
      *Anything you need help with?*
  - trigger: ":shell"
    replace: "{{output}}"
    vars:
      - name: output
        type: shell
        params:
          cmd: echo Hello from your shell